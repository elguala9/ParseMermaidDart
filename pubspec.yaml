name: parse_dart
description: Analyzes Dart projects and generates Mermaid class diagrams.
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  analyzer: ^6.0.0
  glob: ^2.1.2
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
